.DS_Store
.env
.env.*
!.env.example
node_modules/
dist/
build/
out/
.vercel/
.wrangler/
.pytest_cache/
artifacts/
*.log
coverage/
.turbo/
.next/
*.local
package-lock.json.tmp

http-client.private.env.json
.idea/httpRequests/

.bob/

# Astelar knowledge base: repo git independiente anidado, se mantiene aislado
# a propósito (se enlaza desde la página como knowledge base, no se versiona aquí).
apps/fenrir-bridge/astelar/

# Local-only convenience symlink to the separate fenrir-gatekeeper checkout
# (see docs/FENRIR_GATEKEEPER_LOCAL_CHECKOUT.md); never track an absolute path.
apps/fenrir-gatekeeper-worker
