name = "myfenrir-login"
main = "src/worker.js"
compatibility_date = "2026-05-17"

# Vars (non-secret). WORKOS_API_KEY must be set as a SECRET:
#   wrangler secret put WORKOS_API_KEY
[vars]
WORKOS_CLIENT_ID = "client_01KT7NWYWB256XP0V00PX1YW01"
WORKOS_REDIRECT_URI = "https://login.myfenrir.com/auth/callback"
SITE_URL = "https://www.myfenrir.com"

# To serve login.myfenrir.com from this Worker, add a route once DNS for that
# host is on this Cloudflare account:
# routes = [ { pattern = "login.myfenrir.com/*", custom_domain = true } ]
