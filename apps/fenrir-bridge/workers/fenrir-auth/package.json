{
  "name": "fenrir-auth-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Fenrir Better Auth identity Worker for myfenrir.com — folios-auth-worker HTTP contract (Google, Microsoft, Apple).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --config ../../wrangler.fenrir-auth.jsonc",
    "deploy": "wrangler deploy --config ../../wrangler.fenrir-auth.jsonc",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
