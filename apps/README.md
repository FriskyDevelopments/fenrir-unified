# Apps

Expected app imports:

- `fenrir-bridge`: active MyFenrir app, Cloudflare Pages Functions, Workers, auth, Telegram Stars, community gate, readiness checks.
- `fenrir-cinema`: Remotion/admin-guide/video sidecar.

Run `bash ops/materialize-from-sources.sh` from the repo root after the source folders are locally readable.
